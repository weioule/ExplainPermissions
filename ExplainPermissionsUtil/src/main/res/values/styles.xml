<resources>
    <!-- 全屏style -->
    <style name="MyDialogStyle" parent="@android:style/Theme.NoTitleBar.Fullscreen">
        <!-- 是否有边框 -->
        <item name="android:windowFrame">@null</item>
        <!--是否在悬浮Activity之上  -->
        <item name="android:windowIsFloating">true</item>
        <!--标题  -->
        <item name="android:windowNoTitle">true</item>
        <!--阴影  -->
        <item name="android:windowIsTranslucent">true</item><!--半透明-->
        <!-- 点外边可以消失  -->
        <item name="android:windowCloseOnTouchOutside">false</item>
    </style>

    <!-- 继承Theme.AppCompat.NoActionBar，不显示标题栏-->
    <style name="Transparent" parent="Theme.AppCompat.Light.NoActionBar">
        <!--设置窗口的背景为透明，设置透明背景必须要设置此项-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--设置窗口的背景是否为半透明，设置透明背景必须要设置此项-->

        <!--使用这个配置，相当于ExplainPermissionsUtil页面只是浮在外部的MainActivity之上-->
        <item name="android:windowIsTranslucent">true</item>
    </style>

</resources>
